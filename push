#!/bin/bash

# Check if commit message provided
if [ $# -eq 0 ]; then
  echo "Please provide a commit message."
  exit 1
fi

# Add all changes
git add .

# Commit with the provided message
commit_message="$*"
git commit -m "$commit_message"

# Push to the default remote repository (usually 'origin') and the current branch
git push
